name: CACHIX JOB

on:
  push:

jobs:
  push-to-cache:
    name: update-binary-cache
    runs-on: ubuntu-latest
    steps:
      - name: checkput
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: install nix
        uses: cachix/install-nix-action@ba0ddac53f961de1989924298a1f2360edd01669
        with:
          extra_nix_config: |
            extra-experimental-features = nix-command flakes

      - name: setup cachix
        uses: cachix/cachix-action@ad2ddac53f961de1989924298a1f2360edd01668
        with:
          name: your-cache-name
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

      - name: dry build
        shell: bash
        run: |
          nix --extra-experimental-features "nix-command flakes" \
            eval .#default.drvPath --no-pure-eval

      - name: nix build
        shell: bash
        run: |
          nix --extra-experimental-features "nix-command flakes" \
            build .#default --no-pure-eval 
