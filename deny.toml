[advisories]
unmaintained = "all"
yanked = "deny"
ignore = ["RUSTSEC-2025-0141"]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "Unicode-3.0",
    "MPL-2.0",
]

[[licenses.clarify]]
name = "ring"
expression = "MIT AND ISC AND OpenSSL"
license-files = [
    { path = "LICENSE", hash = 0xbd0cc23d }
]

[bans]
multiple-versions = "warn"
wildcards = "deny"

deny = [
    { name = "openssl", reason = "Use native-tls or pure-rust rustls features instead" }
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = [ "registry+https://github.com/rust-lang/crates.io-index" ]
